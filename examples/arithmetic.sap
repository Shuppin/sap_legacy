int x = 4
int z = 3

// Binary operations
x = x + z;
x = x - z + 2;
x = x * z - x;
x = x / z;

// Brackets
x = ((x+3) / (z-1))*4;

// Unary operations
x = -x;
x = x;
x = ----x; // Unary operations are stackable

println(x)
println(z)